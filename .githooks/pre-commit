#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
"${repo_root}/scripts/bump-version.sh"

git add "${repo_root}/VERSION" "${repo_root}/web/version.js" "${repo_root}/docs/version.js"
